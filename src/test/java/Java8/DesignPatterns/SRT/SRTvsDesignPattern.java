package Java8.DesignPatterns.SRT;

public class SRTvsDesignPattern {

    //so SRT is basic and irrespective of what app/tech/solution
    //we are implementing we must use SRT
    //ie., breakdown the code into multiple small chunks and then connect them back
    //SRT means just assign single work to single class

    //Design pattern simply tell us that how do the connect works,
    //or in simple terms which class could speak to another class
    //what will be the responsiblity of each class

    //in real world. SRT and DP works together, means breaks down each class
    //into multiple classes and choose a pattern according to need
    //then connect them back
}
