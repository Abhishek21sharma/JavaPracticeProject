package Java8.DesignPatterns.SRT.FactoryPattern;

public class FactoryPattern {
    //it says that create a new object without exposing the instantiaion logic
    //refer to newly created objects using it's common interface

    //in our code when we did create webDriver, we are doing in a seperate class,
    //that's example of Factory design as it's hiding the logic of instantiation


    //new assignment -> do launch google page for english, french, arabic URL
    //search something and return the results


}
